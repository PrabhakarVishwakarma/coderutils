[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "coderutils"
version = "0.1.0"
authors = [
  {name = "Prabhakar Vishwakarma"}
]

description = "Utility functions package"
readme = "README.md"
requires-python = ">=3.8"